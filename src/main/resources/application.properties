spring.datasource.url= jdbc:postgresql://localhost:5432/playlist?createDatabaseIfNotExist=true
#spring.datasource.url= jdbc:postgresql://${DB_HOST}:5432/${DB_NAME}
# ${POSTGRES_SERVICE_HOST}
spring.datasource.username=candidate
#spring.datasource.username=${POSTGRES_USER}
spring.datasource.password=candidate
#spring.datasource.password=${POSTGRES_PASSWORD}
#spring.jpa.properties.hibernate.jdbc.lob.non_contextual_creation= true
spring.jpa.properties.hibernate.dialect= org.hibernate.dialect.PostgreSQLDialect
# Hibernate ddl auto (create, create-drop, validate, update)
spring.jpa.generate-ddl=true
spring.jpa.hibernate.ddl-auto=create
management.endpoints.web.exposure.include=env,health,metrics,info,logfile
